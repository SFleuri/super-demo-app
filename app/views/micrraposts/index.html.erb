<h1>Listing micrraposts</h1>

<table>
  <tr>
    <th>Content</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @micrraposts.each do |micrrapost| %>
  <tr>
    <td><%= micrrapost.content %></td>
    <td><%= micrrapost.user_id %></td>
    <td><%= link_to 'Show', micrrapost %></td>
    <td><%= link_to 'Edit', edit_micrrapost_path(micrrapost) %></td>
    <td><%= link_to 'Destroy', micrrapost, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Micrrapost', new_micrrapost_path %>
